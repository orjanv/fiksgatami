* Include required (if any) lisence-teksts in application About-screen
* Improwe application flow
* Allow user to select point from map, do not force GPS
* Allow user to input details before sending
* Allow user to select category before sending.
